{
  "installCommand": "npm install",
  "buildCommand": "npm run build --workspace shared && npm run prisma:deploy --workspace server && npm run prisma:generate --workspace server && npm run seed --workspace server && npm run build --workspace client",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
